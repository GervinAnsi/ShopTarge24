@model ShopTarge24.Models.RealEstate.RealEstateImageViewModel

<div class="col-md-4 col-lg-3 mb-3">
    <div class="card shadow-sm">
        <img src="@Model.Image" class="card-img-top rounded" alt="@Model.ImageTitle" asp-append-version="true" />
        <div class="card-body p-2 text-center">
            <small class="text-muted">@Model.ImageTitle</small>
        </div>
    </div>
</div>
